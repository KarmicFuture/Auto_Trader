# LinkedIn Developer App → Auth → Sign in with LinkedIn using OpenID Connect
# Redirect URL must match exactly:
#   http://localhost:3040/api/auth/linkedin/callback

LINKEDIN_CLIENT_ID=
LINKEDIN_CLIENT_SECRET=
LINKEDIN_REDIRECT_URI=http://localhost:3040/api/auth/linkedin/callback
